[package]
name = "zugfolge-infra"
description = "Infra-Release-Pipeline: Domänenmodell des Betriebsgraphen (M1.1) und Import-Pipeline OSM-PBF → Rohgraph (M1.2)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
flate2 = { version = "1.1.9", default-features = false, features = ["rust_backend"] }
zugfolge-determinism = { workspace = true }

[dev-dependencies]
# Nur für den PBF-Fixture-Bau in Integrationstests — der Leser selbst
# braucht flate2 ausschließlich zum Entpacken, siehe [dependencies].
flate2 = { version = "1.1.9", default-features = false, features = ["rust_backend"] }

[lints]
workspace = true
