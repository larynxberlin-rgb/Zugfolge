# ─────────────────────────────────────────────────────────────
# Proprietäre Schichten — dürfen NIEMALS ins öffentliche Repo
# Siehe docs/rechteschutz.md 3 und docs/geschaeft.md 4 (Schichtentrennung).
# Diese Blöcke sind kein Aufräumen, sondern Rechteschutz.
# .gitignore ist die Bitte; der Wächter 'layer-separation' ist der Riegel
# und bricht die CI, wenn eine dieser Schichten doch im Baum landet.
# ─────────────────────────────────────────────────────────────

# Balancing und Wirtschaftsdaten
/data/economy/
/data/balancing/
/data/fahrzeugkatalog/
*.economyrelease
*.economyrelease.json

# Weltdaten, Betriebshistorie, Spielerdaten
/data/worlds/
/data/eventlog/
/data/replays/
/backups/

# OSM-Extrakte, Höhenmodelle und abgeleitete Releases
# (ODbL-Pflichten laufen getrennt — siehe docs/daten.md)
/data/osm/
/data/dem/
/data/infra/
*.pbf
*.osm
*.osm.bz2
*.poly
!tools/region-import/mitteldeutschland-b.poly
*.mbtiles
*.pmtiles
*.tif
*.tiff

# Marken- und Gestaltungsassets
/assets/brand/
/assets/liveries/

# ─────────────────────────────────────────────────────────────
# Geheimnisse
# ─────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
!ops/keys/zugfolge-alpha-2026-ed25519-public.pem
!ops/keys/zugfolge-alpha-2026.3-ed25519-public.pem
!ops/keys/zugfolge-map-deutschland-2026.4-ed25519-public.pem
*.key
*.p12
*.pfx
secrets/
/config/local.*

# ─────────────────────────────────────────────────────────────
# Node / TypeScript
# ─────────────────────────────────────────────────────────────
node_modules/
dist/
build/
coverage/
.turbo/
.vite/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# ─────────────────────────────────────────────────────────────
# Rust
# ─────────────────────────────────────────────────────────────
target/
**/*.rs.bk
*.pdb
# napi-rs Artefakte
*.node

# ─────────────────────────────────────────────────────────────
# Datenbank und lokale Dienste
# ─────────────────────────────────────────────────────────────
/pgdata/
*.sqlite
*.sqlite3
*.dump
docker-compose.override.yml

# ─────────────────────────────────────────────────────────────
# Test- und Werkzeugausgaben
# ─────────────────────────────────────────────────────────────
/test-results/
/playwright-report/
/benchmarks/out/
/var/
/tmp/
/.codex-tmp/
__pycache__/
*.py[cod]
*.profraw
*.heapsnapshot

# Golden-Master-Fixtures werden versioniert — hier bewusst KEIN Ignore.
# Ihre *Ausgaben* dagegen schon:
/tests/**/__actual__/

# ─────────────────────────────────────────────────────────────
# Betriebssystem und Editor
# ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json
.claude/settings.local.json
